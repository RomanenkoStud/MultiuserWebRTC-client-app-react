class FeedbackService {
    rate(rating, email) {
        //api call;
    }

    report(issue, email) {
        //api call;
    }
}

const feedbackService = new FeedbackService();

export default feedbackService;